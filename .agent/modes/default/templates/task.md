<!--
DERIVED FILE — DO NOT EDIT BY HAND.

`task.md` is regenerated from this topic's `tasks.toml` every time task state
changes. The authoritative source for task state is `tasks.toml`. The
checkboxes below are a human-readable rendering only.

To change task state, use the UniSpec MCP tools:

  - task_status { topic, task_id, status }   # mutate one task
  - task_write  { topic, content }           # one-shot import (legacy md only)

Editing the checkboxes here directly will be overwritten on the next
regeneration and will NOT update `tasks.toml`. Notes below the task list are
not authoritative; long-form notes belong in `notes.md`.

Status vocabulary (per task in `tasks.toml`):
  pending | in_progress | complete | blocked

The legacy `working` value is accepted by `task_status` and translated to
`in_progress`.
-->

# Tasks: <TopicName>

<!--
The sections below are produced by the renderer from `tasks.toml`. The
template shape is shown for reference only — the real file is overwritten
when tasks are added, completed, or reordered.
-->

## Implementation

### Phase 1: Foundation
- [ ] **1.1** [Foundation task]

### Phase 2: Core Features
- [ ] **2.1** [Core feature task]

### Phase 3: Integration
- [ ] **3.1** [Integration task]

### Phase 4: Polish
- [ ] **4.1** [Polish task]
